import { createTheme } from 'shuttle-ui';

export default createTheme({
  text: {
    fonts: {
      cochin: { fontFamily: 'Cochin' },
    },
    sizes: {
      xl: 24,
      lg: 20,
      md: 16,
      sm: 12,
      xs: 8,
    },
  },
  props: {
    Text: {
      // size: 'xl',
    },
  },
});
