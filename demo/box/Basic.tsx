import React from 'react';
import { Box, BoxProps } from '@shuttle-ui/components';

export const Title = 'Basic';

export const Example = (props: BoxProps) => {
  return <Box height={100} {...props} />;
};
