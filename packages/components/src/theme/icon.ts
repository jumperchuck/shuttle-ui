import { IconProps } from '../icon';

export type IconTheme = ShuttleUI.ThemeComponent<IconProps>;

const icon: IconTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default icon;
