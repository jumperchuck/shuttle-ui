import { CheckboxProps } from '../checkbox';

export type CheckboxTheme = ShuttleUI.ThemeComponent<CheckboxProps>;

const checkbox: CheckboxTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default checkbox;
