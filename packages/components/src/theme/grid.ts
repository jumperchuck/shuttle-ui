import { GridProps } from '../grid';

export type GridTheme = ShuttleUI.ThemeComponent<GridProps>;

const grid: GridTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default grid;
