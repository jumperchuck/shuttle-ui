import { CardCoverProps } from '../card';

export type CardCoverTheme = ShuttleUI.ThemeComponent<CardCoverProps>;

const cardCover: CardCoverTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default cardCover;
