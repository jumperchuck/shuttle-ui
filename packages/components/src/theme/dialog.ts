import { DialogProps } from '../dialog';

export type DialogTheme = ShuttleUI.ThemeComponent<DialogProps>;

const dialog: DialogTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default dialog;
