import { BoxProps } from '../box';

export type BoxTheme = ShuttleUI.ThemeComponent<BoxProps>;

const box: BoxTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default box;
