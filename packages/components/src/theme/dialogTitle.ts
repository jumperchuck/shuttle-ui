import { DialogTitleProps } from '../dialog';

export type DialogTitleTheme = ShuttleUI.ThemeComponent<DialogTitleProps>;

const dialogTitle: DialogTitleTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default dialogTitle;
