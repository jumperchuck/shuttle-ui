import { DialogInputProps } from '../dialog';

export type DialogInputTheme = ShuttleUI.ThemeComponent<DialogInputProps>;

const dialogInput: DialogInputTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default dialogInput;
