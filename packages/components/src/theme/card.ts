import { CardProps } from '../card';

export type CardTheme = ShuttleUI.ThemeComponent<CardProps>;

const card: CardTheme = {
  defaultProps: {},
  propConfigs: {},
  configPriorities: [],
};

export default card;
